<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <co.sigmoidlabs.bankussdtoolbox.widget.SquareRoundedLinearLayout
        android:id="@+id/bank_background"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:radius="@dimen/bank_item_curve"
        android:gravity="center"
        android:stateListAnimator="@animator/raise"
        tools:background="#B3D619"
        android:foreground="?attr/selectableItemBackground">

        <TextView
            android:id="@+id/text_bank_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textSize="@dimen/text_bank_name"
            tools:text="Diamond Bank"
            android:textColor="@color/text_primary_light"/>

    </co.sigmoidlabs.bankussdtoolbox.widget.SquareRoundedLinearLayout>
</layout>